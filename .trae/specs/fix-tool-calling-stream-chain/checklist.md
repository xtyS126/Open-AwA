- [x] 后端 `agent.py` 的 `process_stream()` 完成了 tool_calls 检测、执行、回注的整个闭环
- [x] 后端 `executor.py` 完成了 `_execute_tool_call()` 和 `_build_tool_message()` 辅助方法
- [x] 后端 `executor.py` 的 `_call_llm_api_stream()` 透传了 `type: "tool_calls"` 事件
- [x] 后端 `executor.py` 的 `_call_llm_api()` 非流式路径支持 tool_calls 循环
- [x] 后端 `litellm_adapter.py` 的 `litellm_chat_completion()` 接受并传递 `tools` 参数
- [x] 后端 `litellm_adapter.py` 的 `litellm_chat_completion_stream()` 接受并传递 `tools` 参数
- [x] 后端 `chat_protocol.py` 新增了 `emit_task_event()` 和 `emit_tool_event()` 辅助函数
- [x] 前端异步兼容性已评审：`handleSend()` 已正确处理 `task`/`tool` SSE 事件
- [x] 前端 executionMeta.ts `applyTaskUpdate()`/`applyToolUpdate()` 已是幂等实现
- [x] `test_litellm_adapter.py` 33 个测试全部通过（含新增的 tool_calls 测试）
- [x] `test_executor_tool_calling.py` 2 个测试全部通过（含 mock 修复后的工具调用闭环测试）
- [x] `test_chat_streaming_status.py` 2 个测试全部通过
- [x] 已确认预存在的 3 个测试失败与本次变更无关

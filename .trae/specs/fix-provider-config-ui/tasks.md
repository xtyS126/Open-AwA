# Tasks

- [x] 任务1：修复供应商详情面板 API URL 字段缺失
  - [x] 检查当前 SettingsPage 中供应商详情面板渲染逻辑
  - [x] 确保 API URL 字段在右侧详情面板中正确渲染且绑定 state
  - [x] 验证切换供应商后 API URL 能正确回填

- [x] 任务2：设计并实现新建供应商弹窗组件
  - [x] 在 SettingsPage 中新增 CreateProviderModal 组件
  - [x] 弹窗包含供应商标识（必填）、显示名称、图标地址、API URL 字段
  - [x] 实现"取消"和"确认创建"两个操作
  - [x] 表单提交前验证供应商标识不能为空

- [x] 任务3：将"新增供应商"按钮行为从内联改为弹窗
  - [x] 移除原有在 div 内联展开的新建逻辑
  - [x] 绑定按钮点击事件以打开弹窗
  - [x] 弹窗创建成功后自动选中新供应商条目

- [x] 任务4：联调验证关键场景
  - [x] 验证已有供应商详情面板字段完整（包含 API URL）
  - [x] 验证新建供应商弹窗可正常打开、填写、提交
  - [x] 验证新建成功后左侧列表更新且新条目被选中
  - [x] 验证空标识提交时有错误提示
  - [x] 运行前端类型检查与构建命令

# Task Dependencies
- 任务2 依赖 任务1 的字段修复，以便弹窗复用相同的字段结构
- 任务3 依赖 任务2 的弹窗组件完成
- 任务4 依赖 任务1、任务2、任务3 全部完成

# Notes
- 弹窗样式应居中、遮罩背景、可通过 ESC 或遮罩关闭
- API Key 字段在新建弹窗中可选（与编辑详情面板保持一致）
- 不新增路由或页面，仅修改现有 SettingsPage 的局部交互

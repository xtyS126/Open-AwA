# Checklist

## UI与交互组件
- [x] 独立的 `ReasoningContent` 组件创建并替换了原来的 `div` 展示。
- [x] 包含明显的折叠标题栏（如带有箭头或文字“思考过程”），点击可以控制展开/折叠。
- [x] 展开高度达到 `320px` 阈值时停止增加高度，触发内部 `overflow-y: auto`。
- [x] 包含 CSS 动画的平滑折叠/展开过渡效果。

## 逻辑与状态管理
- [x] 在 `isStreaming`（流式输出进行中）时，新追加的思维链内容将导致内部滚动条自动定位于最底部，保证最新内容可见。
- [x] 流式输出结束后，该条思维链将默认被折叠，仅展示标题栏。
- [x] 组件能够将用户最后一次点击的展开/收起状态写入 `localStorage` 并在重新加载时正确读取恢复。

## 质量控制与测试
- [x] 编写了单元测试验证组件在 `isStreaming`、`content` 变化时的行为、高度计算及 `localStorage` 持久化逻辑。
- [x] 编写并执行了 E2E 测试脚本，模拟流式输出、达到 `320px` 的滚动情况及折叠/展开行为。
- [x] 通过 ESLint、Prettier、TypeScript 类型检查。
- [x] 动画无卡顿，且支持不同分辨率与主题样式。
# Checklist

## 范围与目标
- [x] 本次变更仅聚焦供应商 API URL 的规范化、模型列表获取与聊天请求后缀补全
- [x] 未引入与本需求无关的设置页大规模重构

## API URL 保存与回填
- [x] 输入以 `/v1` 结尾的地址时，保存后不会自动追加其他后缀
- [x] 输入不带 `/v1` 的地址时，保存后会自动补充 `/v1`
- [x] 输入完整聊天接口地址时，不会出现重复拼接，且能被系统兼容处理
- [x] 重新打开供应商详情面板时，显示的是规范化后的可复用基础地址

## 获取模型列表
- [x] 点击“获取模型列表”时，系统会自动补充模型列表接口所需后缀
- [x] `https://api.deepseek.com/` 作为输入时可以成功获取并展示模型列表
- [x] `https://api.deepseek.com/v1` 作为输入时可以成功获取并展示模型列表
- [x] 模型列表请求不会出现重复 `/v1` 或错误路径拼接

## 聊天请求
- [x] 聊天请求会自动补充聊天接口所需后缀
- [x] 聊天请求不会出现重复 `/chat/completions` 路径
- [x] 现有供应商配置在变更后保持兼容，不因历史值导致聊天失败

## 验证
- [x] 已补充或更新相关测试覆盖 URL 规范化与请求拼接行为
- [x] 前端类型检查通过
- [x] 前端构建或相关测试命令通过
- [x] 如涉及后端改动，相关验证通过

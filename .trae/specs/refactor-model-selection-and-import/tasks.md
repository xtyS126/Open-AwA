# Tasks

- [x] 任务1：后端模型接口重构
  - [x] 子任务1.1：检查并调整“获取模型列表”接口，确保其只负责获取和返回，不自动强制覆盖现有配置。
  - [x] 子任务1.2：实现/调整模型导入（批量保存）接口。
  - [x] 子任务1.3：实现/调整批量删除特定供应商下指定模型的接口。

- [x] 任务2：前端模型导入对话框开发
  - [x] 子任务2.1：在 `SettingsPage` 中创建模型导入 Modal 组件，支持多选列表。
  - [x] 子任务2.2：对接“获取模型列表”按钮，增加 Loading 状态，成功后打开 Modal。
  - [x] 子任务2.3：实现 Modal 中的“确认导入”逻辑，对接后端保存接口，并处理错误提示。

- [x] 任务3：前端配置页模型展示与批量删除
  - [x] 子任务3.1：在供应商配置区域展示已导入的模型列表，并为每项提供 Checkbox。
  - [x] 子任务3.2：新增“批量删除”按钮，当勾选已导入模型时激活。
  - [x] 子任务3.3：实现批量删除的二次确认弹窗及调用后端删除接口的逻辑。

- [x] 任务4：前后端模型状态实时同步
  - [x] 子任务4.1：检查全局状态管理（如 Zustand），确保导入/删除操作后触发状态刷新。
  - [x] 子任务4.2：更新聊天页面（`ChatPage`）的模型选择器（`select`），确保其数据源直接依赖于已导入模型列表，且行为一致。

- [x] 任务5：稳定性与边界情况测试
  - [x] 子任务5.1：验证在网络延迟/断网情况下，获取列表和导入/删除操作的错误提示是否正常。
  - [x] 子任务5.2：验证全流程的 UI 反馈（加载动画、确认弹窗、成功/失败 Toast）。
  - [x] 子任务5.3：运行前端类型检查及后端相关单元测试，确保没有回归问题。

# Task Dependencies
- 任务2 依赖 任务1 的后端接口调整。
- 任务3 依赖 任务1 的后端接口调整。
- 任务4 依赖 任务2 和 任务3 完成前端展示层逻辑。
- 任务5 依赖 所有功能性任务完成。

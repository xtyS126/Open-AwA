- [x] `sandbox.py` 使用 `create_subprocess_exec` 而非 `create_subprocess_shell`
- [x] `sandbox.py` 的 `_ALLOWED_COMMANDS` 白名单不包含 `rm`、`chmod`、`chown`、`xargs`、`awk`、`sed`
- [x] `sandbox.py` 的 `_DANGEROUS_COMMANDS` 黑名单包含所有高危命令
- [x] `sandbox.py` 的 `_DANGEROUS_ARG_PATTERNS` 覆盖路径遍历和命令替换模式
- [x] `skill_executor.py` 的 `_ALLOWED_SHELL_COMMANDS` 已移除高危命令
- [x] `skill_executor.py` 的 `_execute_shell_action` 使用 `shell=False`
- [x] `sandbox.py` 的 `_validate_path` 正确限制路径在工作目录内
- [x] `skill_executor.py` 的 `_validate_file_path` 正确限制路径在工作目录内
- [x] `CodeValidator._ALLOWED_NODE_TYPES` 不包含 `FunctionDef`、`AsyncFunctionDef`
- [x] `CodeValidator._FORBIDDEN_BUILTINS` 包含 `getattr`、`setattr`、`delattr`
- [x] `CodeValidator._SAFE_BUILTINS` 不包含 `getattr`
- [x] `_execute_code_action` 的 `safe_builtins` 不包含 `getattr`
- [x] `backend/tests/test_sandbox_security.py` 测试文件存在且所有测试通过
- [x] `backend/tests/test_skill_executor_security.py` 测试文件存在且所有测试通过
- [x] 安全测试覆盖命令注入、路径遍历、危险函数调用场景

- [x] 侧边栏 `插件` 已迁移为可展开分支，且显示 `插件管理`、`插件配置` 子项
- [x] 路由已采用嵌套结构，`/plugins/manage` 与 `/plugins/config/:pluginId` 可独立访问
- [x] `/plugins/manage` 列表展示名称、版本、作者、状态、简介缩略字段
- [x] 管理页支持关键词搜索与批量删除，删除后列表即时更新且出现 Toast 反馈
- [x] 管理页支持本地 ZIP 与远程 URL 导入，导入结果有成功/失败反馈
- [x] 可查看插件简介详情，详情信息与列表数据一致
- [x] `/plugins/config/:pluginId` 可按 `schema.json` 动态渲染表单
- [x] 动态表单完整支持 `input`、`select`、`switch`、`code-editor`、`file-picker`
- [x] 所有可编辑项具备实时校验，非法值不可保存
- [x] 配置保存后写入 `${pluginId}/config.json`，且不刷新页面实时生效
- [x] `重置默认`、`导出配置`、`导入配置` 三项功能完整并含二次确认弹窗
- [x] 插件异步操作均通过 hooks 封装，并暴露 `loading`、`error`、`retry`
- [x] 单元测试覆盖导入解析、表单渲染、配置持久化三大核心场景
- [x] 相关测试覆盖率达到或超过 80%
- [x] README 已说明目录结构、环境变量、插件包格式与本地调试步骤
- [x] 验收满足：≤50MB 插件导入 <3s、配置表单首渲染 <300ms、连续快照内存增长 ≤1MB

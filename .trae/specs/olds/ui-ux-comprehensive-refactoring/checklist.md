- [x] `UserFloatingArea` 已成功移动到 `Sidebar` 的 `nav` 容器的最底部。
- [x] `UserFloatingArea` 不再使用破坏文档流的绝对/固定定位，且在侧边栏中显示自然。
- [x] 全局色彩搭配已优化，明暗模式下对比度均符合可读性要求。
- [x] 字体排版层次清晰，字号、字重配置合理。
- [x] 所有的交互元素（按钮、侧边栏菜单等）均具有平滑且明显的 hover/active 状态反馈。
- [x] UI 图标统一使用 SVG，未使用 emoji。
- [x] 页面在主要屏幕尺寸（如 1024px, 1440px）下响应式布局正常，不出现内容溢出或错位。
- [x] 代码结构清晰，TypeScript 和 ESLint 静态检查通过。

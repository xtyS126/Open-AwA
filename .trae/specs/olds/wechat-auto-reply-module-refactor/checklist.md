- [x] `CommunicationPage` 已成功重构为左右分栏布局，左侧包含独立的“微信”菜单入口。
- [x] 点击“微信”菜单后，成功动态加载微信独立模块（零外部依赖，热插拔设计）。
- [x] 模块加载时，正确从后端回显了微信的 Account ID、Token、Base URL 以及当前的自动回复规则。
- [x] 可视化表单支持新增、修改、删除自动回复规则，并可通过“一键恢复默认”重置配置。
- [x] 修改配置保存后，调用了统一的配置持久化接口，后台自动回复规则实时生效。
- [x] 后台已实现持续运行的消息监听、规则匹配、内容组装与发送的闭环。
- [x] 单元测试报告生成，覆盖率 ≥90%（重点验证了容错和触发逻辑）。
- [x] 端到端测试（E2E）执行通过，全链路（菜单->保存->生效）无阻断。
- [x] 性能压测报告证明，在 1000条/秒 并发消息下，处理延迟 <200 ms。
- [x] 提交物包含 `CHANGELOG.md`、部署指引及回滚方案文档。
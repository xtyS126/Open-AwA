# Tasks

- [x] 任务1：确认并收敛现有经验页的数据来源与失败点
  - [x] 检查当前经验页调用的前端服务与后端接口
  - [x] 确认触发 500 的链路与需要替换的最小范围

- [x] 任务2：设计并实现文件型经验后端接口
  - [x] 定义 `memory_skill` 目录的实际读取位置与允许的文件类型
  - [x] 提供列表接口：返回 Markdown 文件列表及基础元数据
  - [x] 提供详情接口：返回单个 Markdown 文件完整内容
  - [x] 提供保存接口：更新指定 Markdown 文件内容
  - [x] 增加非法路径与文件不存在时的错误处理

- [x] 任务3：改造 `experience-extractor` 的保存目标
  - [x] 将提取结果从原存储方式改为写入 `memory_skill` 目录
  - [x] 约定文件命名规则，避免重名覆盖
  - [x] 返回保存结果，便于前端或日志确认

- [x] 任务4：重构经验页为文件列表与编辑页
  - [x] 替换旧的数据请求服务，接入新的文件接口
  - [x] 展示 `memory_skill` 下所有 Markdown 文件
  - [x] 支持查看单个文件内容
  - [x] 支持编辑并保存 Markdown 内容
  - [x] 对空目录、加载失败、保存失败提供明确提示

- [x] 任务5：联调并验证关键场景
  - [x] 验证经验页不再出现 500 导致的空白状态
  - [x] 验证目录为空时页面可正常打开
  - [x] 验证编辑保存后内容可再次读取
  - [x] 验证 `experience-extractor` 能生成并写入 Markdown 文件
  - [x] 运行项目已有的相关测试、类型检查与构建命令

- [x] 任务6：修复 `/experience` 路由命中后 `main` 区域空白问题
  - [x] 检查前端路由表是否注册 `ExperiencePage`
  - [x] 让侧边栏 `/experience` 与路由配置保持一致
  - [x] 验证访问 `/experience` 时页面可见并加载文件列表

# Task Dependencies
- 任务2 依赖 任务1 的问题定位结果
- 任务3 可与 任务2 并行推进，但需复用同一目录约定
- 任务4 依赖 任务2 提供稳定接口
- 任务5 依赖 任务2、任务3、任务4 完成
- 任务6 依赖 任务4 的经验页组件已完成

# Notes
- 本次优先实现用户明确要求的“列出、查看、编辑、保存 Markdown 文件”能力
- 本次不扩展搜索、统计、删除、迁移旧数据库经验等非必要能力，除非实现过程中证明其为修复主流程所必需
- 如现有项目已存在可复用的 Markdown 编辑/渲染能力，优先复用，不强制新增依赖

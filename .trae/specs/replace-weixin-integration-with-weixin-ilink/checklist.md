- [x] 已基于 `weixin-ilink/README.md` 与源码完成替换范围与能力映射梳理
- [ ] 后端二维码登录流程已按 `weixin-ilink` 语义重构，并能返回 `botToken`、`accountId`、`baseUrl`、`userId`
- [ ] 后端消息轮询、发送、配置获取与必要状态持久化已与 `weixin-ilink` 能力对齐
- [ ] 前端通讯页已改为消费新的登录状态与字段，不再依赖旧状态名和旧兼容逻辑
- [ ] 与新方案冲突的旧 weixin 集成代码已被删除或替换
- [ ] 后端测试、前端测试、类型检查、lint 与必要构建校验已通过
- [ ] 回归步骤可验证二维码登录、凭据回填、消息拉取与消息发送主路径

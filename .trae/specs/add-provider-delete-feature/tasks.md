# Tasks

- [x] 任务1：梳理删除供应商的现状与边界
  - [x] 确认当前前端可拿到的供应商唯一标识与配置 ID
  - [x] 确认后端现有删除能力仅覆盖 configuration 维度
  - [x] 明确删除后前端选中态与空状态的切换规则

- [x] 任务2：实现后端按供应商删除接口
  - [x] 在 `PricingManager` 新增按 provider 删除激活配置的方法
  - [x] 在 `billing` 路由新增 `DELETE /billing/providers/{provider}` 接口
  - [x] 对不存在供应商返回 404，对删除成功返回 success

- [x] 任务3：补充前端删除供应商能力
  - [x] 在 `modelsApi` 增加删除供应商请求方法
  - [x] 在 `SettingsPage` 增加“删除供应商”按钮与确认交互
  - [x] 删除成功后刷新列表并恢复到可操作状态
  - [x] 删除失败时保留当前编辑状态并提示错误

- [x] 任务4：验证与回归
  - [x] 增加或更新后端测试覆盖按供应商删除流程
  - [x] 手工验证 API 配置页删除主流程与边界流程
  - [x] 运行后端测试、前端类型检查、前端构建

# Task Dependencies
- 任务2 依赖 任务1 的边界定义
- 任务3 依赖 任务2 的后端接口可用
- 任务4 依赖 任务2 与 任务3 完成

# Notes
- 删除供应商采用软删除策略：将该供应商关联配置标记为失活
- 删除操作必须具备二次确认，避免误删
- 不新增页面路由，在现有 SettingsPage 完成交互闭环

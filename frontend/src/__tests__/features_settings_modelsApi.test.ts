import { describe, it, expect } from 'vitest'
import * as module from '@/features/settings/modelsApi'

describe('modelsApi', () => {
  it('loads module', () => {
    expect(module).toBeDefined()
  })
})

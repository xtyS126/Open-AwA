import { describe, it, expect } from 'vitest'
import * as module from '@/features/plugins/pluginTypes.test'

describe('pluginTypes.test', () => {
  it('loads module', () => {
    expect(module).toBeDefined()
  })
})

import { describe, it, expect } from 'vitest'
import * as module from '@/features/experiences/experiencesApi'

describe('experiencesApi', () => {
  it('loads module', () => {
    expect(module).toBeDefined()
  })
})

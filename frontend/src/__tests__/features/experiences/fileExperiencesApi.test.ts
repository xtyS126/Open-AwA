import { describe, it, expect } from 'vitest'
import * as module from '@/features/experiences/fileExperiencesApi'

describe('fileExperiencesApi', () => {
  it('loads module', () => {
    expect(module).toBeDefined()
  })
})

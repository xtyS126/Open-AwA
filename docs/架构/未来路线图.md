# Open-AwA 未来规划路线图

> 本文档基于项目当前实现状态，制定各功能模块的演进计划。
> 最后更新: 2026-07-13

---

## 目录

1. [全局状态概览](#0-全局状态概览)
2. [MCP 协议支持](#1-mcp-协议支持)
3. [SubAgent 子智能体](#2-subagent-子智能体)
4. [插件市场](#3-插件市场)
5. [工作流引擎](#4-工作流引擎)
6. [Chain-of-Thought 改进](#5-chain-of-thought-改进)
7. [多语言模型支持扩展](#6-多语言模型支持扩展)
8. [安全增强](#7-安全增强)
9. [微信集成完善](#8-微信集成完善)
10. [计费系统完善](#9-计费系统完善)
11. [前端 UI 改进](#10-前端-ui-改进)

---

## 0. 全局状态概览

| 模块 | 完成度 | 状态 |
|------|--------|------|
| 内置工具 (Built-in Tools) | 95% | 基本完成，15 个 function calling 工具已就位 |
| 计费系统 (Billing) | 95% | 定价/预算/用量/报表/IQR 异常检测/三级预警全链路已贯通 |
| MCP 协议支持 | 95% | 客户端/管理器/API/前端均已实现，资源读取与 SSE Origin 白名单已落地 |
| 记忆系统 (Memory) | 90% | 三级记忆 + 向量搜索 + 混合检索已完成 |
| 微信自动回复 | 90% | 规则引擎 + AI fallback + 跨频道上下文已实现 |
| 多语言模型支持 | 95% | 8 个提供商、LiteLLM 适配、断路器、Ollama、FailoverManager 故障转移 + 延迟监控 |
| SubAgent 子智能体 | 95% | 三级隔离、生命周期状态机、专业 Agent、图定义持久化、可视化编辑与执行历史已实现 |
| Chain-of-Thought | 100% | 多厂商推理内容解析 + 流式传输 + 复杂度自动评估 + 推理审计 + 内容导出 |
| 定时任务 | 85% | 轮询调度 + AI/插件执行 + 崩溃恢复 |
| 微信集成 | 100% | iLink 绑定、会话、自动回复、实时推送、加密多媒体收发与语音转写已闭环 |
| 前端 UI | 100% | 30+ 页面、设计令牌、响应式布局、i18n 四语言切换、E2E 测试覆盖 |
| 工作流引擎 | 98% | YAML/JSON 定义 + 条件分支 + 模板渲染 + 并行执行 + 子工作流组合 + ReactFlow 可视化编辑 |
| 安全增强 | 95% | RBAC + 审计日志 + 沙箱 + 细粒度权限 + IP 白名单/黑名单 + 用户限流 + 异常检测 + CSRF token |
| 插件市场 | 95% | 注册中心 + API + 前端搜索浏览 + 远端下载 + SHA256 校验 + 语义化版本 |

---

## 1. MCP 协议支持

### 1.1 当前状态

- 完成度: **95%**
- MCP 客户端 (`client.py`) 已实现，支持 stdio 和 SSE 传输
- MCP 管理器 (`manager.py`) 为线程安全单例，支持多服务器生命周期管理
- 协议层 (`protocol.py`) 完整实现 JSON-RPC 2.0 消息格式
- API 路由 (`api/routes/mcp.py`) 提供服务器 CRUD、连接管理、工具发现与调用、资源读取、配置快照与回滚
- 前端 `MCPSettings.tsx` 已完成服务器配置管理 UI
- agent 主流程已集成 MCP 工具（`mcp_{server_id}/{tool_name}` 分发）
- 已支持 MCP `resources/list` 与 `resources/read`，并提供按用户所有权隔离的资源 API
- SSE 传输已在连接前执行 Origin 白名单校验

### 1.2 待完成

#### 1.2.1 前端增强

- MCP 工具调用结果展示 UI
- 工具调用历史记录

### 1.3 验收标准

- [x] 通过 stdio 连接本地 MCP Server
- [x] 通过 SSE 连接远程 MCP Server
- [x] 工具发现正确解析 tools/list 响应
- [x] 工具调用正确传递参数和返回结果
- [x] Agent 主流程可自动选择并调用 MCP 工具
- [x] 前端可配置/管理 MCP Server
- [x] 连接异常时有超时和重连机制
- [x] 支持 MCP resources/list 和 resources/read
- [x] SSE 端点 Origin 校验

---

## 2. SubAgent 子智能体

### 2.1 当前状态

- 完成度: **95%**
- v1.6 完成 `subagent.py` 三级隔离（进程级/协程级/会话级）执行引擎
- 生命周期状态机：created/running/paused/completed/failed/cancelled
- 资源限制：最大并发数、超时、内存上限
- 编排器（Orchestrator）支持子代理并行执行、消息合并、错误事件回调
- API 路由已注册 (`api/routes/subagents.py`)，前端 `SubAgentPage` 已对接
- 已提供 CodeReviewAgent、SearchAgent、DataAnalysisAgent 三个专业节点
- 已实现图定义持久化、ReactFlow 可视化编辑、执行历史与执行日志查看

### 2.2 目标

实现可配置的子智能体系统，用户可定义多个专业 Agent 并编排其协作流程。

### 2.3 实现计划

#### 2.3.1 内置 Agent 节点实现 [已实现]

- 代码审查 Agent（CodeReviewAgent）
- 搜索 Agent（SearchAgent）
- 数据分析 Agent（DataAnalysisAgent）
- 每个 Agent 注册到 SubAgentManager

#### 2.3.2 图定义持久化 [已实现]

- 数据库表 `subagent_definitions` 存储图定义
- API 端点支持图的 CRUD

#### 2.3.3 前端界面 [已实现]

- SubAgent 管理页面：图定义编辑器（可视化 DAG）
- 执行监控：查看节点执行状态、结果与历史记录
- 执行历史记录

### 2.4 验收标准

- [x] StateGraph 引擎支持 DAG 执行
- [x] 并行和串行执行模式
- [x] 三级隔离与生命周期状态机
- [x] 资源限制与编排器
- [x] 至少 3 个内置 Agent 节点可用
- [x] 图定义可持久化和加载
- [x] 前端可创建和编辑 Agent 图
- [x] 执行结果与历史记录可查看

---

## 3. 插件市场

### 3.1 当前状态

- 完成度: **95%**
- 后端注册中心 (`marketplace/registry.py`) 已实现：注册、搜索、分类、分页
- API 路由 (`api/routes/marketplace.py`) 提供 5 个端点
- 前端 `MarketplacePage.tsx` 已完成浏览、搜索、分类筛选
- 内置 4 个种子插件（hello-world, theme-switcher, data-chart 等）
- v1.7 新增：远端插件下载（httpx 异步下载 + SHA256 校验 + ZIP 路径穿越防护）
- v1.7 新增：语义化版本管理（SEMVER 正则解析、版本比较）
- 缺评分系统和社区评论功能

### 3.2 目标

完善插件市场的完整生命周期管理，支持远端仓库下载、版本更新、社区评价。

### 3.3 实现计划

#### 3.3.1 远端插件下载 [已实现]

- [x] 插件包下载与校验（SHA256）
- [x] 依赖解析与安装
- [x] 安装回滚机制

#### 3.3.2 版本管理 [已实现]

- [x] 语义化版本检查
- [x] 插件更新检测与一键升级
- [x] 版本变更日志展示

#### 3.3.3 社区功能

- 插件评分与评论
- 下载量统计
- 插件分类标签

### 3.4 验收标准

- [x] 市场页面可正常加载，展示可用插件列表
- [x] 搜索功能按名称/描述匹配
- [x] 分类筛选器工作正常
- [x] 可从远端仓库下载安装插件
- [x] PluginsPage "浏览插件市场"按钮跳转到市场页面
- [x] 内置示例插件在市场中可见
- [x] 插件版本更新可检测和升级
- [ ] 评分和评论功能可用

---

## 4. 工作流引擎

### 4.1 当前状态

- 完成度: **95%**
- YAML/JSON 定义解析 (`parser.py`)
- 支持 6 种步骤类型：tool（内置工具）、skill（技能）、plugin（插件）、condition（条件分支）、parallel（并行执行）、sub_workflow（子工作流）
- 模板渲染引擎：`{{ context.x }}`、`{{ steps.y }}`、`{{ last_result.z }}`
- AST 安全条件表达式求值
- 执行记录入库，支持错误处理（continue/stop）
- v1.6 新增：并行步骤执行（asyncio.gather 多分支并行，支持 on_error 容错）、子工作流组合（递归执行 + 深度限制 + 参数传递 + 结果回传）
- 已提供基于 ReactFlow 的可视化编辑与图结构预览

### 4.2 目标

增强工作流的执行能力和用户体验，支持并行执行、子工作流组合和可视化编辑。

### 4.3 实现计划

#### 4.3.1 并行执行 [已实现]

- [x] 无依赖步骤自动并行执行
- [x] `parallel` 步骤类型显式定义并行组
- [x] 并行结果聚合

#### 4.3.2 子工作流组合 [已实现]

- [x] `sub_workflow` 步骤类型引用其他工作流
- [x] 参数传递与结果回传
- [x] 递归深度限制

#### 4.3.3 前端可视化编辑器 [已实现]

- 拖拽式工作流编辑器
- 步骤节点连线
- 实时预览执行流

### 4.4 验收标准

- [x] 条件分支步骤正确执行
- [x] 工具/技能/插件步骤正常调用
- [x] 模板变量正确渲染
- [x] 并行步骤同时执行（asyncio.gather）
- [x] 子工作流可嵌套调用（含深度限制）
- [x] 前端可视化编辑器可用

---

## 5. Chain-of-Thought 改进

### 5.1 当前状态

- 完成度: **100%**
- 多厂商推理内容解析：OpenAI o-series (`reasoning_effort`)、Anthropic Claude (`thinking budget_tokens`)、DeepSeek R1、Zhipu GLM
- SSE/WebSocket 流式传输推理内容（`event: reasoning`）
- 前端 `ReasoningContent.tsx` 支持折叠/展开、流式跟踪
- `build_thinking_params()` 将 0-5 深度映射到各厂商参数
- Token 统计已区分 reasoning tokens 和 output tokens
- v1.7 新增：`ComplexityAssessor` 问题复杂度评估器（关键词 + 代码块/数学公式 + 输入长度 + 多问号/URL 多维度评分）
- v1.7 新增：`ReasoningAuditManager` 推理审计管理器（记录/查询/统计/导出）
- v1.7 新增：`/api/cot` 路由（复杂度评估、审计列表/详情/统计、推理内容导出）

### 5.2 目标

完善 CoT 的高级功能，增加深度自动调节和审计能力。

### 5.3 实现计划

#### 5.3.1 深度自动调节

- 根据问题复杂度自动选择推理深度
- 简单问候/事实查询 -> 低深度，数学/编程 -> 高深度
- 用户可覆盖自动选择

#### 5.3.2 推理审计

- 推理过程存储到会话记录
- 推理耗时统计和展示
- 推理内容导出功能

### 5.4 验收标准

- [x] 支持 OpenAI/Anthropic/DeepSeek/Zhipu 的推理内容解析
- [x] SSE 流式传输包含 reasoning 类型消息
- [x] 前端正确展示推理过程内容
- [x] reasoning_effort 参数可配置并生效
- [x] Token 统计区分推理和输出
- [x] 问题复杂度自动匹配推理深度（ComplexityAssessor 多维度评分）
- [x] 推理内容可导出（/api/cot/export/{session_id} 支持 json/markdown）

---

## 6. 多语言模型支持扩展

### 6.1 当前状态

- 完成度: **95%**
- 已支持 8 个提供商：OpenAI、Anthropic、Google (Gemini)、DeepSeek、Alibaba (DashScope/通义千问)、Moonshot (Kimi)、Zhipu (智谱 AI)、Ollama
- `build_provider_request()` 按厂商构建不同请求载荷
- LiteLLM 适配器提供统一接口 + 断路器（5 次连续失败断开 60s）
- Ollama 集成：本地模型发现 (`/api/tags`)、OpenAI 兼容接口
- 模型能力探测（vision、function_calling、reasoning）
- v1.7 新增：`FailoverManager` 故障转移引擎（候选模型链 + should_failover 触发条件判断 + execute_with_failover 自动切换）
- v1.7 新增：`LatencyRecord` 延迟监控（首 token 时间 TTFT、分位数统计 p50/p95/p99、按供应商聚合）

### 6.2 目标

增强模型服务的健壮性和可观测性。

### 6.3 实现计划

#### 6.3.1 自动故障转移 [已实现]

- [x] 主模型不可用时自动切换备用模型
- [x] 按优先级配置备用模型列表
- [x] 故障转移事件通知

#### 6.3.2 延迟与性能监控 [已实现]

- [x] 各提供商响应延迟 benchmark
- [x] 首 token 时间 (TTFT) 统计
- [x] 模型推荐（基于性能/价格比）

### 6.4 验收标准

- [x] 8 个提供商均可正常对话
- [x] Ollama 本地模型可连接和调用
- [x] 模型列表自动发现
- [x] 模型能力标识正确
- [x] 自动故障转移到备用模型
- [x] 延迟 benchmark 面板可用

---

## 7. 安全增强

### 7.1 当前状态

- 完成度: **95%**
- RBAC 权限模型：3 个内置角色 (admin `*`、developer 读写、viewer 只读)
- 审计日志：异步记录，支持筛选和导出 (`api/routes/security.py`)
- 沙箱隔离：20 白名单命令、30+ 黑名单命令、路径穿越防护
- 三级权限检查：auto_approve / user_confirm / admin_only
- 前端安全审计页面 (`SecuritySettings.tsx`)
- v1.5 新增：OPENAWA_API_KEY 强制校验（未配置拒绝启动）、SSRF 加固（BASE_URL 内网/本地 IP 阻断）、CLI 工具 generate_api_key.py 写入 .env.local 后立即 chmod 600
- v1.7 新增：`FineGrainedPermissionManager` 细粒度权限（`resource:action` 模型，30+ 预定义权限，支持 `skill:*` 层级通配符）
- v1.7 新增：`IpAccessController` IP 白名单/黑名单（支持 CIDR 网段，白名单优先级高于黑名单）
- v1.7 新增：`UserRateLimiter` 用户级速率限制（内存滑动窗口）
- v1.7 新增：`AnomalyDetector` 异常行为检测（速率突发 rate_burst、重复失败 repeated_failure）
- v1.7 新增：`CsrfTokenManager` CSRF token 管理（生成/校验/轮换/一次性消费/过期清理）

### 7.2 目标

完善安全体系，增加细粒度权限控制和主动防御能力。

### 7.3 实现计划

#### 7.3.1 细粒度权限 [已实现]

- [x] 从角色字符串匹配升级为 `resource:action` 权限模型
- [x] 权限粒度：`plugin:install`、`skill:execute`、`model:use`、`billing:view`
- [x] 自定义角色创建

#### 7.3.2 主动防御 [已实现]

- [x] 单用户 API 速率限制
- [x] IP 白名单/黑名单
- [x] 异常行为检测（短时间内大量请求）
- [x] CSRF token 自动轮换

### 7.4 验收标准

- [x] RBAC 角色可分配，权限校验生效
- [x] 审计日志页面可查看和筛选
- [x] 沙箱超时控制生效
- [x] `resource:action` 细粒度权限模型
- [x] 单用户速率限制
- [x] IP 白名单支持
- [x] 异常行为检测和告警

---

## 8. 微信集成完善

### 8.1 当前状态

- 完成度: **100%**
- 绑定管理：数据库持久化 (`weixin_bindings` 表)、QR 码登录流程
- 自动回复服务：后台轮询、消息去重、AI fallback 生成
- 规则引擎：关键词 + 正则匹配，完整 CRUD API
- 跨频道上下文：Web UI 会话注入微信回复
- 回复净化：去除 think/reasoning 块、代码围栏过滤
- 前端 IM 渠道页集成微信配置、绑定状态与多媒体消息面板
- Token 使用 Fernet 加密持久化；401 失效会持久化为 `expired` 状态并提示重新扫码
- 自动回复服务通过事件总线发布新消息，后端 WebSocket 端点已完成 Origin 与子协议鉴权
- 已支持图片、语音、视频和文件消息的元数据解析、查询、iLink CDN AES 加密上传与发送
- 入站媒体的 CDN 参数与 AES 密钥仅以密文保存，用户可在前端下载自己的资产
- 语音资产可使用用户已配置且声明 `audio` 输入模态的模型完成转写

### 8.2 目标

增强微信集成的稳定性、安全性和多媒体支持。

### 8.3 实现计划

#### 8.3.1 令牌安全 [已完成]

- [x] Token 加密存储（Fernet）
- [x] Token 过期检测、绑定状态持久化与重新扫码引导
- [x] 上游会话失效时重新扫码绑定；iLink 未提供可安全使用的无交互刷新接口

#### 8.3.2 实时消息推送 [已完成]

- [x] WebSocket 推送新消息通知
- [x] 前端消费 WebSocket 事件并实时刷新消息与资产列表
- [x] 消息查询以服务端持久化资产和会话记录为准，不伪造上游未提供的已读状态

#### 8.3.3 多媒体消息 [已完成]

- [x] 图片消息接收、元数据展示与发送
- [x] 语音媒体 CDN 下载、AES 解密与 ASR 转写
- [x] 文件消息元数据处理、上传与发送

### 8.4 验收标准

- [x] 绑定状态数据库持久化
- [x] 自动回复规则引擎工作正常
- [x] 跨频道上下文注入可用
- [x] Token 加密存储
- [x] Token 过期状态监控与重新绑定引导
- [x] WebSocket 实时消息推送
- [x] 前端实时消息消费与安全资产操作
- [x] 图片/文件消息处理
- [x] 语音消息转文字

---

## 9. 计费系统完善

### 9.1 当前状态

- 完成度: **95%**
- 定价管理：8 个厂商、100+ 模型价格数据 (JSON + DB)
- 用量追踪：输入/输出 token、缓存命中、多模态 token 计算
- 预算控制：日/周/月/年周期、警告阈值、全局/用户/项目/模型级别
- 报表系统：JSON/CSV 导出、按模型/内容类型分析
- 提供商目录：自动合并 JSON 定价与 DB 配置（`source: database|pricing_json`）
- 前端 `BillingPage.tsx` 已完成
- 供应商预填充：新增供应商弹窗支持下拉选择已知厂商
- v1.7 新增：`BudgetAlertService` 用量预警（IQR 异常检测 + 三级预警 + 预算阈值通知）
- v1.7 新增：`CostOptimizationService` 成本优化建议（模型性价比推荐 + 缓存命中率 + 闲置模型检测）

### 9.2 目标

完善计费系统的预警和通知能力。

### 9.3 实现计划

#### 9.3.1 用量预警 [已实现]

- [x] 预算消耗达阈值时主动通知
- [x] 通知渠道：前端 Toast、邮件（可选）
- [x] 每日/每周用量摘要

#### 9.3.2 成本优化建议 [已实现]

- [x] 基于历史用量推荐性价比更高的模型
- [x] 缓存命中率统计和优化建议
- [x] 闲置模型检测

### 9.4 验收标准

- [x] 定价数据覆盖全部 8 个厂商
- [x] 预算控制和周期管理
- [x] CSV/JSON 报表导出
- [x] 供应商目录自动包含 JSON 厂商
- [x] 预算阈值通知
- [x] 成本优化建议

---

## 10. 前端 UI 改进

### 10.1 当前状态

- 完成度: **100%**
- 30+ 主要页面：Login、Dashboard、Chat、Communication、Billing、Marketplace、Plugins、Memory、Skills、Settings、ScheduledTasks、LocalSearch、Test、Experience、UserCenter、Subagents、Tasks、Roles、UserProfile、Workspace、Coding、TTS、Terminal、Inbox、DataDashboard 等
- 30+ 组件：ChatInput、MessageContent、ReasoningContent、ThinkingProcess、TaskTracker、InlineToolCallCard 等
- 设计令牌系统完整（tokens.css CSS 变量）、深色/浅色主题
- 共享组件：Sidebar、PageLayout、LoadingState、ConfirmDialog、Toast、ErrorBoundary
- v1.6 新增：外观定制 Tab、供应商表单优化、SSE 代理修复、主题背景系统
- v1.7 新增：外观 Tab 集成语言切换器（zh-CN/en-US/ja-JP/ru-RU 四语言动态加载）
- v1.7 新增：i18n 语言切换 E2E 测试、响应式布局 E2E 测试覆盖桌面/平板/移动三档断点

### 10.2 目标

提升 UI 一致性、响应式设计和国际化支持。

### 10.3 实现计划

#### 10.3.1 响应式设计

- 移动端适配（断点: 768px, 1024px）
- Sidebar 移动端折叠为汉堡菜单
- 表格/图表自适应宽度

#### 10.3.2 国际化 (i18n)

- 提取硬编码中文文本
- 支持中/英文切换
- 语言偏好持久化

#### 10.3.3 E2E 测试

- Playwright 关键路径测试
- 聊天流程、设置变更、插件安装
- CI 集成

### 10.4 验收标准

- [x] 设计令牌和主题切换
- [x] Toast/ConfirmDialog 统一组件
- [x] 768px 断点下 Sidebar 折叠（汉堡菜单 + 遮罩层 + 路由切换自动关闭）
- [x] 中/英文界面切换（外观 Tab 语言选择器，支持 zh-CN/en-US/ja-JP/ru-RU 四语言）
- [x] E2E 关键路径覆盖（响应式布局 + i18n 语言切换 E2E 测试）

---

## 实施优先级

| 优先级 | 模块 | 状态 |
|--------|------|------|
| P0 | 工作流引擎（执行过程实时观测与调试） | 待实施 |
| P1 | 计费系统完善（用量预警 + 成本优化建议） | v1.7 已完成 |
| P1 | MCP 协议（工具调用结果展示与历史记录） | 待实施 |
| P1 | 微信多媒体消息（图片/语音/文件） | 待实施 |
| P1 | SubAgent 子智能体（实时执行事件与模板生态） | 待实施 |
| P2 | 插件市场（社区评分与评论） | 待实施 |
| P2 | 多语言模型（故障转移 + 延迟监控） | v1.7 已完成 |
| P2 | 安全增强（细粒度权限 + 主动防御） | v1.7 已完成 |
| P3 | Chain-of-Thought（自动深度 + 推理审计） | v1.7 已完成 |
| P3 | 前端 UI（响应式/i18n/E2E） | v1.7 已完成 |

# Open-AwA 前后端 9.2 提升控制证据

> 基线来源：`Open-AwA项目深度调研与多维度评分报告-2026-07-29.md`
>
> 执行原则：评分只在代码、测试、运行服务和浏览器工作流均产生新证据后更新。仅有计划、文档或静态代码不计为通过。

## 目标与评分口径

- 原始综合加权分：7.53 / 10。
- 目标综合加权分：不低于 9.20 / 10。
- 后端能力组目标：不低于 9.20 / 10。
- 前端能力组目标：不低于 9.20 / 10。
- 任何安全、数据完整性、认证或核心聊天阻断缺陷存在时，综合分上限为 8.99。
- 未执行真实浏览器核心工作流时，实战用户体验与 UI/UX 两项不得高于 8.50。

目标分采用原报告权重，目标值为：后端架构 9.2、前端架构 9.2、Agent 9.2、记忆 9.4、插件 9.2、ACP 9.2、安全 9.5、计费 9.2、测试 9.3、文档 9.2、UI/UX 9.3、可访问性 9.2、性能稳定性 9.3、代码质量 9.3、实战体验 9.5。按原权重计算目标综合分为 9.277。


## 工程期望到记录映射

| 工程期望 | 负责领域 | 预期行为 | 记录来源 | 刷新频率 |
| --- | --- | --- | --- | --- |
| 后端核心职责可维护 | architecture-decisions | `ExecutionLayer` 保持兼容门面，模型解析、提示构建、工具执行和流处理由独立协作者承担 | AST 架构测试、模块行数与依赖扫描、后端回归 | 每次核心 Agent 变更 |
| 前端 API 边界清晰 | code-readability-for-agents | `api.ts` 仅聚合导出，领域 API 独立且既有导入保持兼容 | TypeScript 类型检查、API 单测、生产构建 | 每次 API 客户端变更 |
| 预期取消不污染错误面板 | web-release-gates | 组件卸载和参数切换产生的取消不作为错误上报，真实网络失败仍上报 | 客户端单测、后端 client-errors 日志、浏览器控制台 | 每次请求生命周期变更 |
| 危险命令策略单一且一致 | input-validation-and-injection-defense | ACP、终端和沙箱复用同一硬阻断判定，审批策略与硬阻断语义分离 | 安全单测、策略引用扫描、终端与 ACP 集成测试 | 每次命令策略变更 |
| 插件发布状态不漂移 | state-machine-correctness | 热更新只有一个状态权威源，重启后可恢复，回滚覆盖同步和异步生命周期 | 插件状态机测试、持久化恢复测试、服务重启测试 | 每次插件生命周期变更 |
| 记忆行为可配置且可追溯 | caching-and-derived-data | 去重阈值可配置，向量集合迁移不重复告警且不丢元数据更新 | 记忆单测、配置测试、运行日志、隔离向量库测试 | 每次记忆配置或集合变更 |
| Agent 退避和上下文预算有界 | llm-serving-cost-and-latency | 重试抖动有上限，工具结果预算随上下文窗口变化并保留最小可诊断信息 | Agent 单测、属性边界测试、调用追踪 | 每次模型调用策略变更 |
| ACP 子进程最小环境继承 | identity-and-secrets | 父进程环境只按跨平台运行白名单继承，Agent 专用变量必须显式配置 | ACP 服务单测、环境键扫描、子进程启动回归 | 每次 ACP 进程配置变更 |
| 上传入口采用统一安全策略 | input-validation-and-injection-defense | 文件大小、扩展名、魔数、路径和文件名均由共享校验器检查 | 上传路由测试、安全测试、路由引用扫描 | 每次上传入口变更 |
| Provider 凭据生命周期完整 | identity-and-secrets | 独立凭据可完整读取并物理删除，不存在的 provider 在写操作前失败且不产生部分变更 | 计费与认证回归、隔离数据库、真实设置页工作流 | 每次凭据接口或设置页变更 |
| 可访问性成为发布门禁 | accessibility-gates | 核心页面无严重自动化违规，图标按钮有名称，键盘可完成核心流 | axe 结果、键盘 E2E、浏览器可访问性树抽查 | 每次前端发布候选 |
| 核心用户工作流真实可用 | web-release-gates | 登录、对话、流式回复、多轮、历史、设置和快速切页均通过且无页面异常 | 浏览器运行记录、网络失败清单、控制台清单 | 每个验收批次 |

## 调研项翻译

| 原报告问题 | 负责领域 | 非技术项跳过 | 转换后的工程行为 |
| --- | --- | --- | --- |
| `executor.py` God Class | architecture-decisions | 否 | 兼容门面委托给可独立测试的协作者，并以 AST 门禁防止回流 |
| `api.ts` God File | code-readability-for-agents | 否 | 按业务域拆分并通过 barrel 保持调用方兼容 |
| 两套插件热更新机制 | state-machine-correctness | 否 | 建立单一权威状态机与重启恢复协议 |
| 硬阻断清单不一致 | input-validation-and-injection-defense | 否 | 共享硬阻断规则，分别保留审批与白名单策略 |
| 连接池与主动取消噪音 | web-release-gates | 否 | 长请求可取消，取消不报错，页面切换不残留请求 |
| 颜色对比度与键盘覆盖缺失 | accessibility-gates | 否 | 自动对比度、语义名称和键盘 E2E 成为发布记录 |
| “执行团队”或人员配置 | 无 | 是 | 本任务只生成工程变更与验证记录，不评价组织安排 |

## 记录清单

| 记录 | 来源 | 频率 | 保留 |
| --- | --- | --- | --- |
| 基线与复评分表 | 本文件与最终复评报告 | 每个波次结束 | 随仓库长期保留 |
| 变更差异 | Git 工作树和提交 | 每个实现切片 | Git 历史长期保留 |
| 后端验证 | pytest 完整命令与退出码 | 每个后端切片和总验收 | 当日验收报告 |
| 前端验证 | Vitest、typecheck、build 完整命令与退出码 | 每个前端切片和总验收 | 当日验收报告 |
| 服务验证 | `/api/system/ping`、`/api/system/health` 和启动日志 | 每个服务验收 | 当日验收报告 |
| 浏览器验证 | 页面异常、控制台、失败请求、核心工作流断言 | 每个前端波次和总验收 | 当日验收报告 |
| 风险例外 | 本文件例外登记 | 每次发现或到期 | 关闭后保留原因与证据 |

## 当前控制评分卡

| 控制 | 状态 | 证据 | 缺口 | 责任模块 |
| --- | --- | --- | --- | --- |
| 前端 API 与路由边界 | 通过 | `api.ts` 保持薄聚合，领域模块独立；路由迁移到 TanStack Router 并保留兼容适配层；lint 0 error、0 warning，生产构建成功，Vitest 106/106 文件和 647/647 项通过 | 无 | `frontend/src/shared/api`、`frontend/src/router` |
| Zustand equality API 迁移 | 通过 | 9 个实际使用 equalityFn 的 store 均改用 `createWithEqualityFn`；专门回归测试通过；浏览器无弃用警告 | 无 | 前端 store |
| 取消请求与权限 SSE 认证生命周期 | 通过 | Axios 取消与 `AbortError` 回归测试；API Key 登录会在发布认证状态前等待 CSRF 初始化；2 个新增单测先红后绿；真实 Chromium 观察到 ticket 请求同时携带 Authorization、X-CSRF-Token、CSRF Cookie 并返回 200 | EventSource 本身仍需保留卸载 abort 与重连上限 | API client / auth bootstrap |
| 后端执行层拆分 | 通过 | `executor.py` 从 2614 行收敛为 95 行薄兼容门面；配置、模型、工具与步骤执行分别委托给四个单一职责协作者；AST 架构门禁 3/3，直接依赖执行器与 Agent 的组合 177 项通过 | 禁止协作者反向 import `core.executor`，由架构门禁持续防回流 | `backend/core/executor.py` |
| 插件热更新单一权威 | 通过 | `PluginManager._runtime_routes` 已变为 `HotUpdateManager.runtime_routes` 的委托属性；重启恢复与状态权威测试通过；插件组合 136 项通过 | 继续禁止在 `PluginManager` 恢复第二个状态容器 | 插件系统 |
| 命令硬阻断单一来源 | 通过 | 新增 `security.command_hard_block`；ACP、Sandbox、内置终端与终端 API 均委托；安全、终端、SSE/WebSocket 共 108 测试通过 | 更广泛的审批型危险命令仍由各入口按职责处理 | 安全系统 |
| Agent 退避与工具预算有界 | 通过 | RED 复现抖动使 30 秒上限膨胀至 45 秒，修复后最终延迟不超过上限；工具历史预算按模型输入窗口四分之一动态计算；Agent 组合 58 项通过 | 单次工具事件仍有固定展示截断上限，但不替代总上下文预算 | Agent 执行链 |
| ACP 最小环境继承 | 通过 | RED 证明任意父进程变量原会泄漏；改为显式跨平台白名单，受控 Agent 配置仍可覆盖；ACP/终端/PTY/VT 组合 234 项通过 | 真实外部 ACP CLI 调用依赖本机安装与认证 | ACP Host |
| 记忆去重阈值配置化 | 通过 | 配置已注入，记忆组合 276 项通过 | live chat 首次初始化与长会话尾延迟仍需独立运行证据 | 记忆系统 |
| Provider 凭据生命周期 | 通过 | 独立凭据硬删除和完整明文读取测试通过；计费、认证、唯一性与端点解析 66 项通过；真实设置页完成显示、删除和后端 404 复核 | 使用隔离伪密钥，未触碰真实凭据 | 计费与设置页 |
| 显式工作目录沙箱边界 | 通过 | 修复项目级可编辑白名单放宽显式 `working_dir` 的问题；沙箱组合 90 项通过、2 项跳过 | 跳过项保持平台依赖说明 | `backend/security/sandbox.py` |
| 核心浏览器工作流 | 通过 | 全新隔离服务完成 owner 初始化、登录/登出和路由守卫；non-stream、SSE、WebSocket 三传输通过；97 项真实 Chromium 门禁全绿，4 个视口下 Chat、Settings、Dashboard、Plugins、Billing 的页面运行时错误为 0 | 真实模型成功回复仍依赖有效 provider 凭证 | Web 客户端 |
| 页面语义与真实 axe | 通过 | Chat、Dashboard、Settings 在全新 Chromium 中无 serious/critical WCAG 2.2 AA 违规；认证 ticket 同时携带 Authorization、CSRF header/Cookie；4 个视口下核心页面无水平溢出 | 插件管理页仍记录部分小于 44px 的非阻断触摸目标，继续保留为 UI 预算项 | 公共布局与核心页面 |
| 成熟组件与冗余退场 | 通过 | 生产只有一个 `AsyncScheduler`；无消费者 `scheduler` 扩展点、Bilibili 私有第二调度器、公开 `trigger` schema、`data.collector` 生命周期占位和预压缩构建副本已物理退役 | 继续由防回流测试阻止旧类型、旧文件和第二权威恢复 | 后端调度 / 插件扩展 / 生命周期 |
| Agent 单一协调器 | 通过 | `AgentTurnCoordinator` 不再按中文关键词分类或正则抽取实体；旧 `ComprehensionLayer`、`PlanningLayer` 文件、构造和专属测试已删除；Agent 组合 121 项、三传输 4 项通过 | 真实 provider 成功质量与尾延迟仍待有效凭证 | Agent 执行链 |
| 前端全量测试稳定性 | 通过 | 根路径双重重定向最小复现曾产生 52 个无限更新错误并使 worker 增长至约 4 GB；RootGuard 收敛为唯一重定向权威后，106/106 文件、647/647 项通过，无 OOM、无限更新或未处理错误 | 保留根索引结构门禁，禁止重新引入第二个根重定向 | 前端测试与路由 |
| 供应链发布门禁 | 通过 | 完整 `npm audit --audit-level=moderate --registry=https://registry.npmjs.org` 返回 0 vulnerabilities；旧路由包、开发生成器漏洞链及其传递依赖已从 lockfile 移除 | 后续依赖或 lockfile 变化必须重新执行完整审计 | 前端工具链 |

## 2026-08-01 最终复评分

本表只计入当前可复现证据。成熟组件“准备采用”不加分，只有旧实现被删除、单一权威得到验证后才加分。后端能力组约 9.24，前端能力组约 9.21，综合加权 9.24；尚未关闭的例外继续阻断 10 分。

| 维度 | 基线 | 阶段分 | 本轮证据或限制 |
| --- | ---: | ---: | --- |
| 后端架构设计 | 7.5 | 9.0 | 提示构建已拆分，插件与调度状态已收敛，规则式 Agent 内核已退场；`executor.py` 仍约 2630 行，不能按满分计 |
| 前端架构设计 | 7.0 | 9.2 | API God File 已拆为领域模块，barrel 兼容，store equality 完整迁移，生产构建与 635 项 Vitest 全绿 |
| Agent 核心能力 | 8.0 | 9.2 | 单一协调器复用模型原生工具调用；旧规则层生产引用为 0；Agent 121 项与三传输新实例门禁通过 |
| 记忆系统 | 8.5 | 9.4 | 去重阈值配置化且组合 276 项通过；live chat 延迟尚未补证 |
| 插件系统 | 7.0 | 9.2 | `HotUpdateManager` 成为单一权威；无消费者调度扩展点和私有第二调度器已删除 |
| ACP 协议集成 | 8.0 | 9.2 | SDK 降级语义已有文档；子进程改为显式环境白名单；ACP、终端、PTY、VT 组合 234 项通过 |
| 安全体系 | 7.5 | 9.5 | 四入口共享硬阻断策略、ACP 最小环境、严格工作目录边界；安全批次 209 项与 313 项通过，后者 4 项跳过 |
| 计费系统 | 7.5 | 9.2 | Provider 凭据生命周期修复；计费与 Provider 组合 154 项通过 |
| 测试质量 | 7.5 | 9.3 | 后端 8 组共 4414 passed、7 skipped；前端 635/635；三传输 4/4；真实 axe 3/3；测试数据库、向量目录和日志按进程隔离 |
| 文档完整性 | 7.0 | 9.2 | 计划、退场决策、控制证据、例外和最终复评均已同步到当前实现 |
| UI/UX 设计 | 7.0 | 9.2 | 移动端、键盘、颜色、目标尺寸、表单名称与 Chat 嵌套交互均已关闭，并有真实浏览器证据 |
| 可访问性 | 6.5 | 9.2 | Chat、Dashboard、Settings 在真实 Chromium 中无 serious/critical WCAG 2.2 AA 违规 |
| 性能与稳定性 | 7.0 | 9.2 | 重试延迟有界、快速切页完成、权限首请求无 403；Playwright 服务结束后端口释放；长时间测试组仍作为观察项 |
| 代码质量 | 7.5 | 9.1 | API 边界、共享安全策略、提示构建协作者、状态权威和旧内核退役均有防回流测试；执行器兼容门面仍偏大 |
| 实战用户体验 | 7.5 | 9.3 | 初始化、登录、历史、概览、设置、移动端、键盘和三种聊天传输通过；无凭证时返回结构化错误 |
| **综合加权** | **7.53** | **9.24** | 四个原 9.2 阻断项均关闭；有效 provider 成功回复、执行器缩薄、开发工具链 high 和长期运行证据继续阻断 10 分 |

当前 9.24 不是四舍五入结果。它来自原报告权重下的 15 维度复算，并以当前源码的全量测试、全新隔离服务和真实浏览器证据为前提；未执行的真实 provider 成功回复不计入完成项。

## 最终运行收尾复核

- 2026-08-01 使用 Playwright 原生 `webServer` 启动全新隔离后端 `18086`、前端 `15179`，临时 SQLite、Qdrant、日志、工作区和初始化标记均与真实运行数据分离；测试结束后两个端口均释放。
- 三传输门禁共 4 项通过：首次 owner 初始化；non-stream 200 与结构化终态；SSE 包含 status、plan、error 和 `[DONE]`；真实 Chromium WebSocket 接受 bearer 子协议并收到 `response_chunk`、`response`。
- SSE plan 与 WebSocket results 均断言 `purpose=模型原生工具调用与回答`，证明加载的是新 `AgentTurnCoordinator`，不是迁移前遗留服务。
- 可访问性门禁在另一组全新隔离后端 `18087`、前端 `15180` 上共 3 项通过：owner 初始化、首个权限 ticket 的 Authorization/CSRF header/Cookie、Chat/Dashboard/Settings serious/critical 违规为 0；端口随后释放。
- 后端 237 个测试文件按 8 组、每次最多并行 2 组执行，合计 4414 passed、7 skipped、0 failed；`--no-cov` 避免共享 coverage 数据，数据库、向量目录和日志按进程隔离。
- Agent 定向组合 121 项通过；聊天、代理注册、传输、Nginx、扩展协议和冗余退役扩大组合 76 项通过。
- 前端目标构造器测试 3/3；全量首轮出现一次 5 秒并发型瞬态，目标用例单独 1/1 后第二轮为 97/97 文件、635/635 项通过。
- `npm run lint` 为 0 error、53 个既有 warning；`npm run build` 转换 3499 个模块并成功完成；生产构建 `.gz`、`.br` 副本数量为 0，压缩职责留给 Nginx 在线策略。
- `git diff --check` 无错误；4 个 Agent 关键文件内存编译通过；旧规则层、私有调度器和 collector 文件物理残留为 0。
- 既有 8000 用户服务始终未停止；未修改或替换 `var/data/openawa.db`、WAL、SHM 与真实凭据。
- 生产发布依赖审计的既有证据仍为 0 high、2 moderate；完整工具链 4 high、2 moderate 继续按开发生成器隔离例外处理，未执行破坏性强制修复。

## 2026-08-02 增量复评与最终验收

本轮关闭了 2026-08-01 复评中两个最大的可控例外：后端执行器兼容门面和前端完整工具链漏洞；同时清零静态分析告警并修复前端入口测试 OOM。分数仍只计入当前可复现证据，不把缺少 provider 凭证时的结构化错误算作真实模型成功。

| 维度 | 2026-08-01 | 2026-08-02 | 新证据或剩余限制 |
| --- | ---: | ---: | --- |
| 后端架构设计 | 9.0 | 9.5 | `executor.py` 为 95 行薄门面，四类职责由独立协作者承担，AST 防回流门禁 3/3 |
| 前端架构设计 | 9.2 | 9.5 | 领域 API 保持薄聚合；路由迁移到成熟路由器并保留兼容层；根路径只有一个重定向权威 |
| Agent 核心能力 | 9.2 | 9.2 | 单一协调器和三传输继续通过；真实 provider 成功质量仍待凭证 |
| 记忆系统 | 9.4 | 9.4 | 既有去重与隔离证据保持；live chat 尾延迟未新增长期证据 |
| 插件系统 | 9.2 | 9.2 | 单一热更新与调度权威保持，未恢复已退役冗余 |
| ACP 协议集成 | 9.2 | 9.2 | 最小环境与协议降级证据保持 |
| 安全体系 | 9.5 | 9.5 | 共享硬阻断、CSRF、Origin、隔离目录和凭据边界保持 |
| 计费系统 | 9.2 | 9.2 | Provider 凭据生命周期证据保持 |
| 测试质量 | 9.3 | 9.5 | 后端 4417 passed、7 skipped；前端 647/647；真实 Chromium 97/97；run-all 9/10，唯一失败为隔离环境缺少模型凭证 |
| 文档完整性 | 9.2 | 9.3 | 历史基线、控制证据、关闭项、阻塞项和当前验证结果已互相链接 |
| UI/UX 设计 | 9.2 | 9.2 | 多视口核心页面通过；插件管理页仍有小于 44px 的触摸目标观察项 |
| 可访问性 | 9.2 | 9.4 | Chat、Dashboard、Settings 的 serious/critical WCAG 2.2 AA 违规为 0，多视口页面错误为 0 |
| 性能与稳定性 | 9.2 | 9.4 | 修复根路由无限更新和约 4 GB OOM；全量 Vitest 23.52 秒完成；大分块和长期运行仍是观察项 |
| 代码质量 | 9.1 | 9.6 | 执行器薄门面、ESLint 53 降至 0、无新增 `any`/规则降级/suppression，`git diff --check` 通过 |
| 实战用户体验 | 9.3 | 9.4 | 初始化、认证、守卫、核心页面、三传输和移动视口真实通过；真实模型成功回复仍待有效凭证 |
| **综合加权** | **9.24** | **9.36** | 按原报告 15 维权重计算为 9.358；真实 provider、长稳证据、触摸目标和大分块继续阻断 10 分 |

本轮实时验证证据：

- 后端执行器架构门禁 `3 passed`；后端代码在执行器提交后未再变化，既有 8 组全量证据为 4417 passed、7 skipped、0 failed。
- `npm run lint` 为 0 error、0 warning；`npm run typecheck`、`npm run build` 均通过；完整 `npm audit --audit-level=moderate` 为 0 vulnerabilities。
- 前端全量为 106 个测试文件、647 项测试全部通过；原 OOM 的 `main.test.tsx` 与 `App.a11y.test.tsx` 独立通过。
- 隔离后端 `18088` 与前端 `15181` 上真实 Chromium 97/97：首次初始化、认证与守卫、non-stream、SSE、WebSocket、CSRF、axe、多视口页面错误和水平溢出门禁全部通过。
- 内置 `run-all` 在另一全新隔离实例中为 9/10；唯一失败为 `chat-nonstream` 的 `llm_api_key_missing`，其余 9 个服务、诊断与生命周期场景通过。该结果保留为外部凭据阻塞，不伪造成功。
- 用户服务端口 `8000` 全程保持停止；隔离端口 `18088/15181/18090/15183` 均在测试后释放，未修改或替换 `var/data/openawa.db`、WAL、SHM、向量库与真实凭据。

## 例外登记

| 例外 | 剩余风险 | 补偿控制 | 接受权限 | 到期时间 | 刷新触发 |
| --- | --- | --- | --- | --- | --- |
| 真实模型调用依赖有效 provider 凭证 | 无法证明真实 LLM 回复质量和尾延迟 | 验证无凭证错误路径、SSE 协议与取消；有凭证时必须补跑真实对话 | 项目所有者 | 2026-08-06 | 检测到可调用 provider 或凭证变化 |
| Vite 大块与 TTS 混合导入警告 | 仍有大于 300 kB 的 Markdown、Recharts、Terminal 等分块 | 生产构建成功；压缩由 Nginx 在线处理；后续单独做分块预算治理 | 前端性能门禁 | 2026-08-05 | bundler 配置或依赖变化 |
| 移动端插件管理触摸目标预算 | 真实 Chromium 在 `/plugins/manage` 记录 32 个小于 44px 的候选目标，其中包含侧栏项和图标按钮 | 页面无水平溢出、关键 UI 可见且自动化门禁通过；后续单独收紧触摸目标断言并修复候选控件 | 前端 UI 门禁 | 2026-08-06 | 移动布局或导航变更 |
| 后端第 5 测试组偏慢 | 该组 703 项耗时约 400 秒，拖慢反馈 | 保留全量通过结果；单独分析慢测试，不通过放宽业务超时或限流语义提速 | 后端测试门禁 | 2026-08-04 | 测试配置、限流或慢场景变更 |

## 证据包目录

| 章节 | 来源工件 | 保留 | 复核者 |
| --- | --- | --- | --- |
| 基线与目标 | 2026-07-29 调研报告、本文件 | 长期 | 项目维护者 |
| 代码与架构 | Git diff、AST 测试、依赖扫描 | 长期 | 变更复核 |
| 测试与构建 | pytest、Vitest、typecheck、Vite 输出 | 当日验收报告 | 质量门禁 |
| 运行服务 | ping、health、启动失败清单、错误日志 | 当日验收报告 | 运行验收 |
| 浏览器工作流 | 工作流断言、页面错误、控制台与请求失败 | 当日验收报告 | 用户体验验收 |
| 最终复评 | 15 维度复评分与未通过项 | 长期 | 项目维护者 |

## 标准更新待办

| 缺口来源 | 工程期望 | 严重度 | 预期修复路径 | 目标日期 |
| --- | --- | --- | --- | --- |
| 最新错误日志 | 取消请求不作为 ERROR 上报 | 已关闭 | 客户端统一分类并用浏览器快速切页验证 | 2026-07-30 |
| 前端全量测试 | 懒加载测试不依赖并发时序 | 已关闭 | 使用 5 秒受控可观察边界，95 文件 629 项通过 | 2026-07-30 |
| 原报告后端架构 | 执行层职责可独立演进 | 高 | 分离模型解析、提示构建、流处理和工具执行 | 2026-08-02 |
| 原报告插件系统 | 热更新状态单一且可恢复 | 已关闭 | `PluginManager` 只委托，`HotUpdateManager` 保持唯一状态权威并覆盖恢复 | 2026-07-31 |
| 原报告 Agent 设计 | 删除关键词理解与规则规划冗余 | 已关闭 | 单一协调器委托模型原生工具调用；旧模块、构造和专属测试删除；禁止双跑门禁与三传输通过 | 2026-08-01 |
| 调度器与扩展点扫描 | 生产只保留一个 `AsyncScheduler`，抽象必须有消费者 | 已关闭 | scheduler 扩展点、Bilibili 私有占位调度器、公开 trigger 和虚假文档入口全部删除 | 2026-08-01 |
| 原报告 Agent 核心 | 重试与上下文预算有界 | 已关闭 | 最终抖动延迟封顶；动态工具预算与 7 个流式子方法形成组合回归 | 2026-07-30 |
| 原报告 ACP 集成 | 子进程只继承最小环境 | 已关闭 | 显式跨平台环境白名单，Agent 专用变量改为配置声明 | 2026-07-30 |
| 原报告计费系统 | Provider 凭据读取和删除全生命周期一致 | 已关闭 | 修复部分变更错误，补独立凭据硬删除、明文读取、组合回归和真实设置页验收 | 2026-07-30 |
| 原报告安全体系 | 命令硬阻断规则无漂移 | 已关闭 | 共享模块加四入口一致性测试 | 2026-07-30 |
| 原报告可访问性 | 核心页面满足自动与键盘门禁 | 已关闭 | Chat 嵌套交互已拆为同级原生控件；Chat、Dashboard、Settings 真实 axe serious/critical 为 0 | 2026-08-01 |
| 前端全量测试 | 当前测试集全绿 | 已关闭 | 构造器 mock 改为 class；第二轮全量 97 文件、635 项通过 | 2026-08-01 |
| 供应链审计 | 发布候选无 high | 高 | 升级、替换或隔离 OpenAPI 生成器链，升级 React Router 后重新审计 | 2026-08-04 |
| 原报告实战体验 | 当前关键场景无页面异常 | 已关闭至无凭证边界 | 全新服务完成 non-stream、SSE、WebSocket 与真实 Chromium 验收；真实模型成功质量保留为凭证例外 | 2026-08-01 |

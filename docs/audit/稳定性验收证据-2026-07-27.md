# Open-AwA 稳定性验收证据

> 验收日期：2026-07-27
> 对应清单：[稳定性短板清单-2026-07-26.md](稳定性短板清单-2026-07-26.md)
> 运行态验证仅使用临时 SQLite：`C:/Users/23941/AppData/Local/Temp/openawa_stability_20260727.db`，未替换、删除或写入真实运行库。

## 验收结论

20 项均已具备代码修复和至少一项定向验证证据。聊天的真实模型生成不在本次验收范围：隔离库没有可调用的 provider API Key；本次只验证认证、SSE 建连、任务标识和 WebSocket 握手/释放。

| 编号 | 验收重点 | 代码与定向证据 | 结论 |
| --- | --- | --- | --- |
| Critical-1 | SSE 取消、流收尾、工具超时、reader 释放 | `test_stability_critical_paths.py`、`test_stream_orchestrator.py`、前端全量 Vitest；隔离 SSE 返回 `200 text/event-stream` | 通过 |
| Critical-2 | 启动组件失败可降级 | `test_stability_critical_paths.py` 覆盖启动失败记录 | 通过 |
| Critical-3 | 超时分类与错误脱敏 | `test_stability_critical_paths.py` 验证 504 结构化响应且不泄露内部细节 | 通过 |
| Critical-4 | 路由和局部错误降级 | `ChatPage.test.tsx` 验证 MessageList 渲染失败时输入区域仍可用；前端全量 Vitest、`main.test.tsx`、`router/index.test.tsx` | 通过 |
| Critical-5 | API 超时、401 和损坏 localStorage 降级 | 前端全量 Vitest、TypeScript 检查和生产构建 | 通过 |
| Warning-1 | 有界全局状态和单例初始化 | `test_ws_manager.py`、定向后端回归集 | 通过 |
| Warning-2 | 登出清理与跨账号隔离 | `authLogoutIsolation.test.ts`、前端全量 Vitest | 通过 |
| Warning-3 | ACP 队列、取消等待和进程清理 | `test_stability_critical_paths.py` 与 ACP 路由定向回归 | 通过 |
| Warning-4 | 子进程与 PTY 的有界 wait 和 LRU 释放 | `test_terminal_pty.py`，含 kill 后仍不退出的回归场景 | 通过 |
| Warning-5 | 初始化失败和实时通道降级 | `useAppInitialization.test.ts`、`test_discussion_routes.py` | 通过 |
| Warning-6 | task runtime 数据库 I/O 不阻塞事件循环 | `runners.py` 与 `facade.py` 的短生命周期 Session/查询均经 `asyncio.to_thread`；`test_task_runtime_phase1.py` 覆盖运行器和外观层委派 | 通过 |
| Warning-7 | Stream reader 的 releaseLock/cancel | `api.test.ts` 覆盖聊天流正常结束和读取异常时的 releaseLock；ACP 流实现同样在 finally 释放 reader | 通过 |
| Warning-8 | HTTP 客户端、任务和权限流资源释放 | `test_im_adapter_resource_cleanup.py`、`test_stability_critical_paths.py` | 通过 |
| Warning-9 | failover、retry 和搜索链路总时限 | `test_retry.py`、`test_web_search_multi_provider.py` | 通过 |
| Warning-10 | WebSocket 异常路径断连 | `test_ws_manager.py`；隔离认证 WebSocket 成功握手并主动关闭 | 通过 |
| Suggestion-1 | 日志保留异常堆栈 | 定向后端回归集 | 通过 |
| Suggestion-2 | HMR 全局监听清理 | `main.test.tsx`、前端全量 Vitest | 通过 |
| Suggestion-3 | 内置插件种子事务原子性 | 启动定向回归与隔离后端启动 | 通过 |
| Suggestion-4 | 推理计时器状态切换和卸载清理 | `ReasoningContent.test.tsx` | 通过 |
| Suggestion-5 | inbox 多标签 leader 协调 | `inboxStream.test.ts` | 通过 |

## 已执行命令

```powershell
cd backend
python -m pytest --no-cov tests/test_stability_critical_paths.py tests/test_im_adapter_resource_cleanup.py tests/test_retry.py tests/test_web_search_multi_provider.py tests/test_stream_orchestrator.py tests/test_ws_manager.py tests/test_discussion_routes.py tests/test_task_runtime_phase1.py tests/test_task_runtime_phase2.py tests/test_task_runtime_phase3.py tests/test_task_runtime_phase4.py
# 245 passed

python -m pytest --no-cov tests/test_terminal_pty.py tests/test_stability_critical_paths.py tests/test_im_adapter_resource_cleanup.py tests/test_retry.py tests/test_web_search_multi_provider.py tests/test_stream_orchestrator.py tests/test_ws_manager.py
# 103 passed

cd ../frontend
npx vitest run
npm run typecheck
npm run build
```

当前轮额外完成了隔离后端 `GET /api/system/ping`、认证 SSE 建连、认证 WebSocket 握手，以及 Chromium 前端页面加载验证。

后续复核补齐了聊天主区局部 ErrorBoundary、聊天流 reader 锁释放断言，以及 TaskRuntimeFacade 的同步数据库/存储调用线程化；已执行 `python -m pytest --no-cov tests/test_task_runtime_phase1.py`（54 passed）与 `npx vitest run src/__tests__/features/chat/ChatPage.test.tsx`（11 passed）、`npx vitest run src/__tests__/shared/api/api.test.ts`（16 passed）、`npm run typecheck`、`npm run build`。

# bilibili-toolkit-builtin 内置插件第三方依赖清单
# 版本下限对齐参考项目 OpenBiliClaw v0.3.147 的 pyproject.toml
# 安装方式: pip install -r backend/plugins/bilibili_toolkit_builtin/requirements.txt

httpx>=0.27
pydantic>=2.0
loguru>=0.7
bilibili-api-python>=16
google-genai>=1.66
ollama>=0.4
openai>=1.0
anthropic>=0.40

# OpenBiliClaw 完整运行还依赖以下包，但 Open-AwA 仅启用 OpenClaw 适配层时
# 可以延后安装；缺失会以 loaded_with_warnings 状态加载，不阻塞启动。
# 如需启用全部能力（抖音/YouTube/X/小红书发现、桌面打包等），请参考
# 参考项目 OpenBiliClaw/pyproject.toml 的 [project.optional-dependencies] 段补装。
